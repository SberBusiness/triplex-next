/** Направление кнопки навигации пагинации. */
export enum EPaginationNavigationIconDirection {
    /** Переход к предыдущей странице. */
    BACK,
    /** Переход к следующей странице. */
    NEXT,
}
