import { Meta, Title, Heading } from "@storybook/addon-docs/blocks";

<Meta title="release-notes/v1/1.47.0" />

<Title>v1.47.0</Title>

<Heading>Изменения</Heading>

**AmountField**

- Добавлено: `clearProps` — свойства кнопки очистки. Нужен, чтобы задать ей доступное имя (`aria-label`): кнопка содержит только иконку, а зашить дефолтную строку нельзя — библиотека мультиязычная. Обработчик клика по-прежнему задаётся через `onClear`.
- Проведён AI-рефакторинг: у компонента появился `displayName` (виден в React DevTools). Других изменений для потребителя нет.

**Avatar**

- Проведён AI-рефакторинг: изменений для потребителя нет.

**CardStatic**

- Добавлено: `CardStatic` пробрасывает `ref` на свой корневой `<div>` — раньше компонент был объявлен как `React.FC`, поэтому переданный `ref` терялся. Тип компонента изменился на `React.ForwardRefExoticComponent`.
- Проведён AI-рефакторинг: других изменений для потребителя нет.
