export enum EFormFieldSize {
    SM = "sm",
    MD = "md",
    LG = "lg",
}

export enum EFormFieldStatus {
    DEFAULT = "default",
    DISABLED = "disabled",
    ERROR = "error",
    WARNING = "warning",
}
