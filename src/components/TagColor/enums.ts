/** Статус тега, определяющий цвет фона. */
export enum ETagColorStatus {
    /** Нейтральный — статус по умолчанию. */
    DEFAULT = "default",
    /** Успешное завершение. */
    SUCCESS = "success",
    /** Информационное сообщение. */
    INFO = "info",
    /** Предупреждение. */
    WARNING = "warning",
    /** Ошибка. */
    ERROR = "error",
}
