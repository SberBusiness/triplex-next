/** Типы компонента Island. Отличаются цвет и тень. */
export enum EIslandType {
    /** Основной тип: непрозрачный фон, без тени. */
    TYPE_1 = "type_1",
    /** Полупрозрачный фон с внутренней рамкой. */
    TYPE_2 = "type_2",
    /** Приглушённый фон, без тени. */
    TYPE_3 = "type_3",
}
