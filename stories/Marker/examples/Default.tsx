import React from "react";
import { Marker, EMarkerStatus, EComponentSize } from "@sberbusiness/triplex-next";

export const Default = () => <Marker status={EMarkerStatus.SUCCESS} size={EComponentSize.MD} />;
