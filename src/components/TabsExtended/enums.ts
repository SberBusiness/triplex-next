/** Тип оформления табов: задаёт фон контейнера и кнопок. Выбирается по фону страницы, на которой стоят табы. */
export enum ETabsExtendedType {
    /** Серый фон контейнера — для табов на белом фоне страницы. */
    TYPE_1 = "type1",
    /** Белый фон контейнера — для табов на затенённом фоне страницы. */
    TYPE_2 = "type2",
}
