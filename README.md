# Triplex-next

## 🚀 Установка

```bash
npm install triplex-next
```

---

## 📁 Структура

```
src/
  components/
    MyComponent/
      MyComponent.tsx
      MyComponent.module.css
      index.ts
      __tests__/MyComponent.test.tsx
```

> Каждый компонент должен иметь собственный `index.ts`, чтобы поддерживать генерацию `exports` в `package.json`.

---

## 📜 Скрипты

| Скрипт | Назначение |
|-------|------------|
| `npm run rollup-build` | Сборка компонентов и `style.css` в `dist/` |
| `npm run storybook` | Локальный просмотр компонентов |
| `npm run storybook:build` | Сборка Storybook в `storybook-static/` |

---

## 🧱 Добавление нового компонента

1. Создай папку:

```bash
src/components/Alert/
```

2. Добавь файлы:

```
Alert.tsx
Alert.module.css
index.ts         ← обязательный!
```

3. Зарегистрируй в `src/index.ts`:

```ts
export * from './components/Alert';
```

---
## 🧪 Тестирование

Проект использует [Vitest](https://vitest.dev/) для юнит-тестирования и [Testing Library](https://testing-library.com/docs/react-testing-library/intro/) для тестирования компонентов.
Для скриншот тестов - Loki.

### Как запустить тесты

```bash
npm run test-e2e         # Запуск e2e тестов
npm run test-unit         # Запуск unit тестов
npm run test-unit:watch   # Запуск unit тестов в режиме наблюдения
npm run test-unit:coverage # Запуск unit тестов и генерация отчёта о покрытии
npm run test-visual # Запуск скриншот тестов. Тесты по всем описанным story.
npm run test-visual:update # Обновление изображений скриншот тестов
npm run test-visual:gallery # Запуск генерации gallery.html с информацией об изменениях. Изменения - http://127.0.0.1:8080/gallery.html. Запускать после того, как в test-visuals будут упавшие тесты.
```

## 📤 Экспорт компонентов

> При сборке `package.json` в `dist/` генерируется автоматически скриптом.  
Основан на наличии `src/components/<Name>/index.ts`.

---

## 📦 Публикация

```bash
npm run build
npm publish
```
