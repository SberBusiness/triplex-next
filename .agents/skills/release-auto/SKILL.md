---
name: release-auto
description: Предполётные проверки и автоматический запуск релиза triplex-next из еженедельной routine — проверяет ветку-маркер prerelease-1.Y.0, непустые release notes, готовность release-0 (перелитие сделано, CI зелёный) и запускает release в unattended-режиме. Нет ветки-маркера — штатный выход без релиза.
---

# release-auto

Точка входа для еженедельной cloud-routine. Сам ничего не публикует: делает
предполётные проверки и, если все сошлись, запускает
[`release`](../release/SKILL.md) с `UNATTENDED=true`.

Смысл разделения: `release` умеет выпускать, `release-auto` решает, **можно
ли выпускать сегодня**. Все решения принимаются до первой необратимой
операции — после запуска `release` откатить публикацию уже нельзя.

## Когда запускать

Из routine по расписанию (обычно раз в неделю). Вручную имеет смысл
запустить, чтобы проверить готовность веток, не выпуская релиз, — но тогда
останови процесс после отчёта предполёта.

Для ручного релиза с известной версией используй `release` напрямую.

## Что должно быть сделано человеком

Регламент подготовки — [`docs/human/release.md`](../../../docs/human/release.md).
Кратко, в порядке выполнения:

1. Все задачи спринта смержены в `main`, дальше `main` заморожен до релиза.
2. `stories/release-notes/v1/<VERSION>.mdx` заполнен.
3. Запушена ветка-маркер: `git push origin main:refs/heads/prerelease-1.Y.0`.
4. `main` перелит в `release-0`, конфликты React 17-адаптаций разрешены,
   ветка запушена, CI на ней зелёный.

Порядок 1 → 4 обязателен: коммит, попавший в `main` после перелития, уронит
проверку `is-ancestor`, и релиза не будет.

---

## 1. Ветка-маркер

```bash
git fetch origin --prune
git ls-remote --heads origin 'refs/heads/prerelease-*'
```

- **Ничего не найдено** → штатный выход. Это не ошибка: на этой неделе
  релиза нет. Отчёт в одну строку, дальше не идти.
- **Ровно одна ветка** `prerelease-1.Y.0` → это `VERSION`.
- **Несколько веток** → стоп. Непонятно, что выпускать; разбирается человек.

Из `VERSION` выводятся `V0` = `0.Y.0`, `NEXT` = `1.(Y+1).0`,
`NEXT0` = `0.(Y+1).0`.

## 2. Предполётные проверки

Любая не сошлась — стоп с отчётом. Ничего не публикуется, ничего не чинится
молча: каждая из этих проверок означает, что подготовка не завершена, а
достраивать её за человека нельзя.

```bash
git status --porcelain
git show origin/main:package.json | grep -m1 '"version"'       # версия main
git show origin/release-0:package.json | grep -m1 '"version"'  # версия release-0
gh release view <VERSION> 2>&1 | head -3
gh release view <V0> 2>&1 | head -3
git rev-parse origin/main
git merge-base --is-ancestor origin/main origin/release-0
gh run list --workflow=ci.yml --branch release-0 --limit 1 \
  --json headSha,status,conclusion,url
git rev-parse origin/release-0
```

| Проверка | Условие |
|---|---|
| Рабочее дерево | чистое |
| Формат версии | `VERSION` = `1.Y.0`, минор ровно на 1 больше текущего в `main`, патч `0` |
| Релизы | ни `VERSION`, ни `V0` ещё не выпущены |
| Миноры веток | минор в `main` и в `release-0` совпадает |
| Release notes | `stories/release-notes/v1/<VERSION>.mdx` существует и не заготовка — предикат в [`release-react18`](../release-react18/SKILL.md), шаг 1 |
| Перелитие | `is-ancestor origin/main origin/release-0` проходит |
| CI `release-0` | последний прогон `ci.yml` на `release-0`: `headSha` = вершина `origin/release-0`, `status` = `completed`, `conclusion` = `success` |

При провале `is-ancestor` покажи, чего именно не хватает, — иначе сообщение
бесполезно:

```bash
git log origin/main --not origin/release-0 --oneline
```

Прогон CI на другом коммите, чем вершина ветки, **не считается зелёным**:
проверялось не то состояние `release-0`.

Зафиксируй `MAIN_SHA` = `git rev-parse origin/main` на этот момент.
`is-ancestor` дальше не перепроверяется: мерж релизного PR добавит в `main`
коммит с бампом `1.Y.0`, которого в `release-0` нет и быть не должно.

## 3. Задача Linear

`TASK` — номер задачи Linear на релиз, формат `TRI-XXX`. Найди открытую
задачу «Релиз <VERSION> / <V0>»; если её нет — заведи через skill
`create-task`.

Linear MCP в headless-запуске может быть недоступен. Тогда **не** выдумывай
номер и не подставляй legacy-префикс: сообщи об этом в отчёте и остановись —
без `TASK` релизные коммиты не соответствуют
[`docs/ai/commits.md`](../../../docs/ai/commits.md).

## 4. Запуск релиза

Запусти [`release`](../release/SKILL.md), передав:

| Вход | Значение |
|---|---|
| `VERSION` | из ветки-маркера |
| `V0`, `NEXT`, `NEXT0` | выведены на шаге 1 |
| `TASK` | из шага 3 |
| `UNATTENDED` | `true` |
| `MAIN_SHA` | зафиксирован на шаге 2 |

Дальше всё ведёт `release`: React 18-половина, гейт на её успешность,
React 17-половина. Вмешиваться в его шаги не нужно — при остановке под-скила
просто передай его отчёт дальше.

## 5. Отчёт

Один итоговый отчёт в любом исходе. Обязательно должно быть видно, **что
именно произошло**, без чтения логов:

- **Релиза не было** — какая проверка не сошлась и что нужно доделать.
  Отсутствие ветки-маркера формулируй как штатный пропуск, а не как ошибку.
- **Релиз выпущен** — то, что вернул `release`: обе версии, ссылки на PR и
  GitHub Release, `dist-tags` (`latest` = `VERSION`, `react17` = `V0`),
  каким путём пошли release notes React 17-половины, наличие ассета
  `mcp-data-<VERSION>.json`, ссылки на Storybook.
- **Релиз выпущен частично** — что опубликовано, что нет и почему. Это самый
  важный случай: `1.Y.0` уже в npm и отозвать её нельзя, поэтому в отчёте
  должно быть явно сказано, что React 17-половина осталась невыпущенной и
  доделывается через `release-react17`.

## Жёсткие ограничения

- Не выпускать релиз при любой не сошедшейся предполётной проверке.
- Не доливать `main` в `release-0` и не чинить красный CI — подготовка веток
  целиком на человеке.
- Не удалять ветку-маркер вручную: она уходит вместе с мержем релизного PR.
- Не перепроверять `is-ancestor` после мержа релизного PR в `main`.
- Не выдумывать номер задачи Linear, если MCP недоступен.
