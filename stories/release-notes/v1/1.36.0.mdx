import { Meta, Title, Heading } from "@storybook/addon-docs/blocks";

<Meta title="release-notes/v1/1.36.0" />

<Title>v1.36.0</Title>

<Heading>Ломающие изменения</Heading>

**LoaderScreen**

- Удалено свойство `theme`, всегда используется `ELoaderSmallTheme.BRAND`.

**ModalWindow / LightBox**

- Свойство `loadingTitle` заменено на `loaderScreenProps`.

<Heading>Изменения</Heading>

**Link**

- При `target="_blank"` автоматически добавляется `rel="noopener"` (защита от reverse tabnabbing). Явно переданный потребителем `rel` сохраняется.

**LoaderScreen**

- Добавлены свойства `description` и `controls` для отображения контента под спиннером.

**ThemeProvider**

- Предупреждение о неподдержке динамической темы при SSR выводится через `console.warn` вместо `console.log`.

**TooltipLink**

- При `target="_blank"` автоматически добавляется `rel="noopener"` (защита от reverse tabnabbing). Явно переданный потребителем `rel` сохраняется.
