/** Тип компонента статус-трекера. */
export enum EStatusTrackerType {
    DRAFT = "draft",
    WAITING = "waiting",
    WARNING = "warning",
    REJECTED = "rejected",
    APPROVED = "approved",
}

/** Вертикальное выравнивание блоков статус-трекера. */
export enum EVerticalAlign {
    TOP = "top",
    MIDDLE = "middle",
    BOTTOM = "bottom",
}
